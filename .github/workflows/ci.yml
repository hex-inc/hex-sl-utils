name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

env:
  UV_VERSION: "0.12.2"
  DEVBOX_VERSION: "0.18.0"

jobs:
  check-and-build:
    name: Check and build
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Devbox
        uses: jetify-com/devbox-install-action@a0d2d53632934ae004f878c840055956d9f741b0 # v0.14.0
        with:
          devbox-version: ${{ env.DEVBOX_VERSION }}
          enable-cache: "true"
      - name: Sync workspace
        run: devbox run setup
      - name: Check packages
        run: devbox run check
      - name: Run package tests
        run: devbox run test:packages
      - name: Run workspace tests
        run: devbox run test:workspace
      - name: Check generated artifacts
        run: devbox run verify
      - name: Build package distributions
        run: devbox run build
      - name: Smoke-test package distributions
        run: devbox run smoke-test

  # Devbox pins Python 3.14 and does not support Windows runners; keep the
  # compatibility matrix on uv's selected interpreter instead.
  test:
    name: Test (${{ matrix.os }}, Python ${{ matrix.python }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            python: "3.9"
          - os: ubuntu-latest
            python: "3.10"
          - os: ubuntu-latest
            python: "3.11"
          - os: ubuntu-latest
            python: "3.12"
          - os: ubuntu-latest
            python: "3.13"
          - os: ubuntu-latest
            python: "3.14"
          - os: macos-latest
            python: "3.14"
          - os: windows-latest
            python: "3.14"
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install uv and Python
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: ${{ env.UV_VERSION }}
          python-version: ${{ matrix.python }}
          enable-cache: true
          cache-dependency-glob: uv.lock
          prune-cache: true
      - name: Sync workspace
        run: uv sync --locked --all-packages
      - name: Run package tests
        run: uv run --locked --all-packages pytest packages/hex-sl-utils/tests -m 'not database'
