-- === CALCS ===
-- today()
-- now()

-- === BIGQUERY ===
CURRENT_DATE;
CURRENT_TIMESTAMP();

-- === CLICKHOUSE ===
CURRENT_DATE();
toTimeZone(CURRENT_TIMESTAMP(), 'America/New_York');

-- === DUCKDB ===
CURRENT_DATE;
CURRENT_TIMESTAMP;

-- === MSSQL ===
CAST(GETDATE() AS DATE);
GETDATE();

-- === MYSQL ===
CURRENT_DATE;
CURRENT_TIMESTAMP();

-- === POSTGRES ===
CURRENT_DATE;
CURRENT_TIMESTAMP;

-- === REDSHIFT ===
CURRENT_DATE;
GETDATE();

-- === SNOWFLAKE ===
CURRENT_DATE;
CURRENT_TIMESTAMP();

-- === SPARK ===
CURRENT_DATE;
CURRENT_TIMESTAMP();

-- === TRINO ===
CURRENT_DATE;
CURRENT_TIMESTAMP;
